<h3 class="productName"><%= @product.name %></h3>
<div class="productDescription"id="product-<%= @product.id %>"><%= @product.description %></div>
<div class="productPrice">$<%= @product.price %></div>
<div class="productInventory">Left in stock: <%= @product.inventory %></div>
<a href="#"class="js-next" data-id="<%= @product.id %>">Next Product</a>

<script type="text/javascript" charset="utf-8">
    $('.js-next').on('click', function() {
        var nextId = parseInt($(".js-next").attr("data-id")) + 1;
        $.getJSON("/products/" + nextId + "/data", function(data) {
            $(".productName").text(data["name"])
            $(".productDescription").text(data["description"])
            $(".productPrice").text(data["price"])
            $(".productInventory").text(data["inventory"])
            $(".js-next").attr("data-id", data["id"]);
        });
    });
</script>