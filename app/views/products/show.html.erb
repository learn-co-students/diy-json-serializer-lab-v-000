
<a href="#" class="js-next" data-id="<%=@product.id%>"> Next Product </a>

<h1 id="productName"><%=@product.id %>: <%= @product.name %></h1>
<p id="productDescription"><%= @product.description %></p>
<p id="productInventory"><%= @product.inventory %></p>

<script type="text/javascript" charset="utf-8">
  $(function () {
    $(".js-next").on("click", function() {
        var nextId = parseInt($(".js-next").attr("data-id")) + 1;
      $.getJSON("/products/" + nextId + "/data", function(data) {
        $("#productName").html(data["name"]);
        $("#productDescription").html(data["description"]);
        $("#productInventory").html(data["inventory"]);

      $(".js-next").attr("data-id", data["id"]);

    });return false;
  });
});
</script>
