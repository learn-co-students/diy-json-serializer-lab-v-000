<h1 class="productName"><%= @product.name %></h1>
<p class="productPrice"><%= @product.price %>$</p>
<p class="productDescription"><%= @product.description %></p>
<p class="productInventory"><%= @product.inventory %> available</p>
<a href="#" class="js-next" data-id="<%= @product.id %>">Next Product</a>

<script type="text/javascript" charset="utf-8">
	$(function(){
		$(".js-next").on("click", function(){
			var nextId = parseInt($(".js-next").attr("data-id")) + 1;
			$.get("/products/" + nextId + "/data", function(data) {
      			$(".productName").html(data["name"]);
      			$(".productPrice").html(data["price"] + " $");
      			$(".productDescription").html(data["description"]);
      			$(".productInventory").html(data["inventory"] + " available");
      			// re-set the id to current on the link
      			$(".js-next").attr("data-id", data["id"]);
      		});
		});
	});
</script>