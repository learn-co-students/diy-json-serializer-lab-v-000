<a href="#" class="js-next-product" data-id="<%=@product.id%>">Next Product</a>
<h1 class="productName"><%= @product.name %></h1>
<h2 class="productPrice">Price: $<%= @product.price %></h2>
<h2 class="productInventory"><%= @product.inventory%> in stock!</h2>
<h3 class="productDescription"><%= @product.description %></h3>

<script type="text/javascript" charset="utf-8">
  $(function() {
    $(".js-next-product").on("click", function() {
      var nextId = parseInt($(".js-next-product").attr("data-id")) + 1;
      $.getJSON("/products/" + nextId + "/data", function(data) {
        $(".productName").text(data["name"]);
        $(".productPrice").text(data["price"]);
        $(".productInventory").text(data["inventory"]);
        $(".productionDescription").text(data["description"]);
        $(".js-next-product").attr("data-id", data["id"]);
      });
    });
  });
</script>
