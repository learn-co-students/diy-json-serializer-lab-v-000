<h1 class="productName"><%= @product.name %></h1>
<p>  <a href="#" class="js-next" data-id="<%=@product.id%>">Next Product</a> </p>
<p class="productPrice"><%= @product.price %></p>
<p class="productInventory"><%= @product.inventory %></p>
<p class="productDescription"><%= @product.description %></p>

<script type="text/javascript" charset="utf-8">
  $(function () {
    $(".js-next").on("click", function() {
      var nextId = parseInt($(".js-next").attr("data-id")) + 1;
      $.getJSON("/products/" + nextId + "/data", function(product) {
        $(".productName").text(product["name"]);
        $(".productInventory").text(product["inventory"]);
        $(".productDescription").text(product["description"]);
        $(".productPrice").text(product["price"]);

        $(".js-next").attr("data-id", product["id"]);
      });
    });
  });
</script>