<h1 class="productName"><%= @product.name %></h1>
<h3>Description</h3>
<p class="productDescription"><%= @product.description if !!@product.description%></p>
<h3>Price</h3>
<p class="productPrice"><%= @product.price if !!@product.price %></p>
<h3>Inventory</h3>
<p class="productInventory"><%= @product.inventory if !!@product.inventory %></p>
<a href="#" class="js-next" data-id="<%=@product.id%>">Next Product</a>



<script type="text/javascript" charset="utf-8">
  $(function () {
    $(".js-next").on("click", function () {
      var nextId = parseInt($(".js-next").attr("data-id")) + 1
      $.getJSON("/products/" + nextId + "/data", function (data) {
        $(".productName").text(data["name"])
        $(".productDescription").text(data["description"])
        $(".productPrice").text(data["price"])
        $(".productInventory").text(data["inventory"])
      
        $(".js-next").attr("data-id", data["id"])
      })
    })
  })
</script>